.ipynb_checkpoints/
.pixi/
__pycache__/
_build/
docs/.buildinfo
outputs/
runs/
.private/
index.md
# Compacted session transcripts. They carry the conversation verbatim, which
# includes home paths and private dataset locations, so they stay out of git.
temp/session_extracts/
