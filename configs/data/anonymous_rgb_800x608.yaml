# A separately materialized, metadata-free RGB ImageFolder.  It lives on the
# workspace volume so the 21-channel long run does not consume the Desktop
# overlay.  The path may be relocated through FRAPPE_DATA_ROOT_800X608.
root: ${oc.env:FRAPPE_DATA_ROOT_800X608,/workspace/data/frappe_rgb_800x608/imagefolder}
